function sum(x, y) {
  return x + y
}

let sum1 = function (x, y) {
  return x + y
}

let sum2 = (x, y) => {
  return x + y
}

function sum3(x: number, y: number): number {
  return x + y
}

let sum4 = (x: number, y: number): number => {
  return x + y
}